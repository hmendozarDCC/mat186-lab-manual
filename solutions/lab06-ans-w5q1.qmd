---
title: "Solution - Lab 6: Data collection"
subtitle: "MAT 186: Introduction to Data Science"
format:
  html:
    toc: true
    toc-location: right
---

<div id="password-gate" style="background: #f8f9fa; padding: 20px; border: 1px solid #dee2e6; border-radius: 5px; text-align: center;">
  <h3>🔒 Solutions Protected</h3>
  <p>Please enter the classroom code to unlock Lab results.</p>
  <input type="password" id="pass-input" style="padding: 5px;" placeholder="Enter password...">
  <button onclick="checkPassword()" style="padding: 5px 15px; background: #007bff; color: white; border: none; border-radius: 3px; cursor: pointer;">Unlock</button>
</div>

<div id="solution-content" style="display:none;">

## Data collection - Official Solutions

::: {.callout-note}
### Instructor Note
Add your specific DCC lab answers below this box.
:::

### Tasks 1-4: Data Acquisition & Organization
The student's project folder must be organized correctly for the code to run. 

**Requirements:**
1. **Source:** [NY State Open Data Portal](https://data.ny.gov/).
2. **File Format:** CSV.
3. **Location:** The file `race_ny.csv` must be moved from "Downloads" into the `data/` folder inside the RStudio Project.



---

### Task 5: Data Narrative (The Paragraph)
*This is an example of what the student should have written in their Quarto document:*

> "This dataset, **race_ny.csv**, was obtained from the NY State Open Data Portal. The data was collected by New York State government agencies to track demographic participation. One row in this dataset represents a single recorded observation of an individual or event entry categorized by race and ethnicity for a specific year and location."

---

### Task 6: Importing and Displaying Data
The student must use `read_csv()` (from the `tidyverse` package) and `head(..., n = 10)`.

::: {.callout-important}
## Path Reminder
Since this solution is inside the `solutions/` folder, we use `../data/` to access the data folder. In the student's main lab file, they would likely just use `data/race_ny.csv`.
:::

```{r}
#| message: false
#| warning: false
# 1. Load the necessary library
library(tidyverse)

# 2. Load the data using the professional folder path
# Replace 'race_ny.csv' with the actual name of your downloaded file
ny_data <- read_csv("../data/race_ny.csv")

# 3. Show the first 10 rows
head(ny_data, n = 10)
```
<br><br>
</div>

<script>
function checkPassword() {
var userPass = document.getElementById("pass-input").value;

// This uses the LAB_PASSWORD from your security.js file
if (userPass === LAB_PASSWORD) {
document.getElementById("password-gate").style.display = "none";
document.getElementById("solution-content").style.display = "block";
} else {
alert("Incorrect password. Please try again.");
}
}

// Allow "Enter" key to trigger the button
document.getElementById("pass-input").addEventListener("keypress", function(event) {
if (event.key === "Enter") {
checkPassword();
}
});
</script>



***

::: {.content-block}
<center>
<a href="../solutions.html" class="btn btn-primary btn-lg" role="button">🏠 Back to Solutions Hub</a>
</center>
:::
